# ZAP alert rules policy.
# Format: <alert_id>\t<WARN|FAIL|IGNORE|INFO>
# Used by zaproxy/action-baseline, action-full-scan and action-api-scan
# to control whether an alert passes or fails the build.
# Rules NOT listed here default to WARN: they fail the build when detected.
#
# Only IGNORE confirmed false positives and INFO accepted/acceptable findings.
#
# Each non-comment line MUST have at least 3 tab-separated columns:
# <alert_id>\t<level>\t<annotation>  (zap_common.load_config requires it)
10049	INFO	(Storable and Cacheable Content - static SPA cacheable by design)
10109	INFO	(Modern Web Application - ZAP states no changes are required)
90005	IGNORE	(Sec-Fetch-* request headers are sent by the browser and cannot be set server-side)