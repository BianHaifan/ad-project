spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/adproject?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC}
    username: ${DB_USERNAME:adproject}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false
