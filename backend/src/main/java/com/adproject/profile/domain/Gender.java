package com.adproject.profile.domain;

/** Controlled self-declared gender for a candidate profile. Null means "not set". */
public enum Gender {
    MALE,
    FEMALE,
    OTHER,
    PREFER_NOT_TO_SAY
}
