import type {ReactNode} from 'react';
export function LoadingState({label='Loading workspace…'}:{label?:string}){return <div className="state-card loading-state" aria-live="polite"><span className="spinner"/><span>{label}</span><span className="loading-skeleton" aria-hidden="true"><i/><i/><i/></span></div>}
export function ErrorState({onRetry,error}:{onRetry:()=>void;error?:unknown}){const requestId=errorRequestId(error);return <div className="state-card error"><strong>Something went wrong</strong><span>We could not load this content.</span>{requestId&&<small>Request ID: {requestId}</small>}<button className="button secondary" onClick={onRetry}>Try again</button></div>}
export function EmptyState({title='Nothing here yet',description='There is no data to display.',action}:{title?:string;description?:string;action?:ReactNode}){return <div className="state-card"><strong>{title}</strong><span>{description}</span>{action}</div>}
function errorRequestId(error:unknown){if(typeof error==='object'&&error!==null){const requestId=(error as {requestId?:unknown}).requestId;return typeof requestId==='string'?requestId:''}return''}
