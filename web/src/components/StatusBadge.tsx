import type {ApplicationStatus,JobStatus} from '../models/recruiter';const labels:Record<ApplicationStatus|JobStatus,string>={APPLIED:'New',IN_REVIEW:'In review',INTERVIEW:'Interview',REJECTED:'Rejected',WITHDRAWN:'Withdrawn',DRAFT:'Draft',ACTIVE:'Active',PAUSED:'Paused',CLOSED:'Closed'};export function StatusBadge({status}:{status:ApplicationStatus|JobStatus}){return <span className={`badge ${status.toLowerCase()}`}>{labels[status]}</span>}
