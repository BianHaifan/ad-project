# Project rules

本文件约束组员和 Codex 在本仓库中的长期协作行为。

## 开始修改前

- 先阅读 `docs/product-requirements.md`、`docs/architecture.md` 和与任务相关的规范。
- 明确任务目标、允许修改目录、API 契约、Figma 节点、验收标准和测试。
- 若需求与规范冲突，先更新并评审规范，不得在代码中悄悄形成第二套规则。

## 技术边界

- 核心后端必须使用 Java + Spring Boot。
- 求职者客户端必须使用 Kotlin Android；默认采用 Jetpack Compose。
- 招聘者和管理员端使用 React + TypeScript。
- 业务数据由 MySQL 保存；客户端不得直接访问数据库。
- Python 只用于团队自训练推荐模型的数据处理、训练、评估与推理，不承载核心身份认证和招聘业务。
- Spring Boot 是对客户端公开的唯一业务 API。

## API 与数据

- API 必须与 OpenAPI 定义一致。
- 不直接把 JPA Entity 暴露为请求或响应 DTO。
- 所有新接口必须进行认证、角色和资源所有权检查。
- 数据库结构通过迁移脚本修改，不依赖手工改库。
- 时间以 UTC 存储；枚举和错误码在各端保持一致。
- 不允许前端通过临时 mock 字段反向定义正式 API。

## 安全

- 不提交密码、Token、API Key、生产连接串或其他密钥。
- 不在日志中记录完整 JWT、密码或不必要的简历敏感信息。
- Agent 工具必须复用正常业务服务的权限校验。
- Agent 可以执行当前用户本人有权完成的白名单业务操作，但不得获得额外权限。
- 有副作用的 Agent 操作必须展示变更预览并先获得用户明确确认。
- Admin 高风险操作必须记录原因和审计日志。

## UI

- Android 和 Web 页面必须处理 loading、empty、error、content 和 submitting/disabled 状态。
- UI 实现引用具体 Figma Frame；设计缺失时先明确需求，不凭空扩展产品范围。
- 第一优先级是 MVP 垂直流程，不提前开发社区、课程、会员等 P2 功能。
- 视觉实现完成后使用截图或真机对比进行校正。

## 测试与完成标准

- 完成功能后运行与改动相关的 lint、typecheck、test 和 build。
- 新接口至少测试成功、未登录、错误角色、错误所有权和主要业务冲突。
- Bug 修复应尽可能增加能复现问题的回归测试。
- 未运行的测试必须在交付说明中明确列出原因，不能声称已通过。
- 功能只有满足 `docs/testing-plan.md` 的完成定义才可标记完成。

## 修改范围

- 不擅自更换技术栈、数据库、认证方式或核心状态机。
- 不让多个任务同时随意修改相同文件或同一模块。
- 保留用户和组员已有改动；不要清理与当前任务无关的工作区内容。
- 不使用破坏性 Git 命令覆盖他人工作。
- 生成代码必须进入约定目录，并保留可重新生成的源契约。

## 任务交付说明

每次交付至少说明：

- 完成了什么
- 修改了哪些模块
- API 或数据库是否变化
- 运行了哪些测试及结果
- 仍存在什么限制
- 下一步安全且最小的工作是什么
