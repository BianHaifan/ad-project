FROM python:3.12-slim

WORKDIR /app
COPY agent-service/pyproject.toml agent-service/README.md ./
COPY agent-service/src ./src
RUN pip install --no-cache-dir .

EXPOSE 8090
CMD ["uvicorn", "agent_service.main:app", "--host", "0.0.0.0", "--port", "8090"]
